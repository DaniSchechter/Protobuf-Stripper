#ifndef confientialDataRule.h
#include <iostream> 
#include <unordered_map> 
#include <stdio.h>
#include <ctime>
#include <chrono>
#include <fstream>
#include <string>
#include <vector>
#include <sstream>

#endif

bool checkIfMaliciousIp(const std::string& dstIp);

bool checkIfDataConfidential(const std::string& data);